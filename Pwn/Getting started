# Getting started

This was another guided challenge (the last) simulating the exploitation of a basic buffer overflow vulnerability. By sending a big enough amount of bytes to overwrite the `0xdeadbeef` address we get the flag.

## HTB{b0f_s33m5_3z_r1ght?}
