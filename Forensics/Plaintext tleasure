# Plaintext tleasure

This was the first, simple forensics challenge. Using Wireshark to inspect the HTTP traffic in the PCAP we find the plaintext flag inside the 4th TCP stream.

## HTB{th3s3_4l13ns_st1ll_us3_HTTP}
